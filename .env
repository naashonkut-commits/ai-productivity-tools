# Environment variables for AI-Powered Productivity Tools

# JWT Secret for authentication (change in production)
JWT_SECRET=dev-secret-do-not-use-in-production

# Mobile Money Payment Configuration
MOBILE_MONEY_PHONE=0761485613
MOBILE_MONEY_NETWORK=MTN

# Node Environment
NODE_ENV=development

# Database URL (if using Prisma with database, not needed for SQLite)
# DATABASE_URL="file:./dev.db"
